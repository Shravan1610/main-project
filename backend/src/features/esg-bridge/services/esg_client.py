"""
backend/src/features/esg-bridge/services/esg_client.py
Calls the ESG model hosted on Render and parses the response.

Owner: Shravan
Task: SH-1-13
Phase: 1

Expected functions:
  fetch_esg_scores(entity_name: str) -> ESGData
    — Sends entity to ESG model API on Render, parses response into ESGData.
    — Falls back to fallback scores if model is unavailable.
"""
# Stub — implement in SH-1-13
