"""
backend/src/features/esg-bridge/services/esg_normalizer.py
Normalizes ESG scores to a consistent 0-100 range.

Owner: Shravan
Task: SH-1-14
Phase: 1

Expected functions:
  normalize_score(raw_score: float, source_range: tuple[float, float] = (0, 10)) -> float
    — Maps a raw score from source_range to 0-100.
  normalize_esg_data(data: ESGData) -> ESGData
    — Normalizes all scores within an ESGData object.
"""
# Stub — implement in SH-1-14
