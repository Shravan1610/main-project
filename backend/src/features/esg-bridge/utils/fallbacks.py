"""
backend/src/features/esg-bridge/utils/fallbacks.py
Fallback ESG scores when the external model is unavailable.

Owner: Shravan
Task: SH-1-15
Phase: 1

Expected functions:
  get_fallback_esg(entity_name: str) -> ESGData
    — Returns sensible default ESG scores (industry averages) when the
      Render-hosted model is down or times out.
  FALLBACK_SCORES: dict — Default score mappings by industry/sector.
"""
# Stub — implement in SH-1-15
