"""
backend/src/features/climate-intel/utils/geo_helpers.py
Coordinate validation, distance calculation, and region mapping.

Owner: Afham
Task: AF-2-15
Phase: 2

Expected functions:
  validate_coordinates(lat: float, lng: float) -> bool — Checks valid lat/lng range.
  haversine_distance(lat1: float, lng1: float, lat2: float, lng2: float) -> float — Distance in km.
  get_region(lat: float, lng: float) -> str — Maps coordinates to a named region/country.
"""
# Stub — implement in AF-2-15
