# GreenTrust Backend Dependencies

# Core framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic>=2.10.0,<3.0.0
pydantic-settings>=2.6.0,<3.0.0

# HTTP client for external APIs
httpx==0.27.0
beautifulsoup4==4.12.3
playwright==1.52.0
# NOTE: Playwright requires browser binaries to be installed separately.
# Run the following in any environment (CI, Docker, local) where Playwright scraping is used:
#   playwright install --with-deps chromium
# Playwright scraping is opt-in via the PLAYWRIGHT_ENABLED=true environment variable.
# It is disabled by default; httpx is always tried first.

# Environment
python-dotenv==1.0.1
python-multipart==0.0.9

# PDF parsing
PyPDF2==3.0.1

# Caching
cachetools==5.3.3

# Testing
pytest==8.1.1
pytest-asyncio==0.23.6

# CORS
# (included in FastAPI via starlette)
